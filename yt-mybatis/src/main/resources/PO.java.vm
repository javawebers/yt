package ${modulePackage}.${packagePO};

${importBaseEntity}

/**
 * ${codeName} PO
 *
 * @author  ${author}
 */
##@JsonSerialize(include= JsonSerialize.Inclusion.NON_NULL)
public class ${poClassName}<T extends ${poClassName}<T>> ${extendsBaseEntity} {
${fieldList}
}
